<p id="notice"><%= notice %></p>
<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>
<div id="tasks">
  <%= render(@list.tasks) %>
</div>
<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
